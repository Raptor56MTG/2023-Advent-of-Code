use std::fs::File;
use std::io::prelude;

fn main() -> i32 {

    // file we are reading from
    let mut file :File = File::open("example1.txt").expect("Failure to read file.");
    let mut contents = String::new();

    4
}
